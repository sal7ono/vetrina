 <form  action="{{ path('recherche') }}" method="POST">
     <div class="search_box pull-right" >
     {{form(form.recherche)}}
                        
     <button type="submit" class="btn btn-default"><img src="{{asset('images/home/searchin.png')}}"/></button>
         {{form(form)}}                
     </div> </form>